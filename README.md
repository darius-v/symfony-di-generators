Todo:
- retest readme

To launch:

`cd docker`
`docker compose run --rm app composer install`

Build:

`docker build -t my-php-app .`

Run
`docker compose up -d`

Bash:
`docker compose exec php bash`


